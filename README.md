# AI Video Generator Backend

Ye backend AI video generation app ke liye Railway deploy ready hai.

## Endpoints

- `GET /` → Test server
- `POST /generate-video` → JSON body me `{ "prompt": "your prompt" }`

## How to Run Locally
1. `npm install`
2. `npm start`
